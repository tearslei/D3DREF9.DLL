# 完整处理器还原：下一阶段执行计划

## 阶段 0：版本和基线冻结

- 固定 `crossfire.exe/cshell.dll 1.1.85.7` SHA-256。
- 保留原 `D3DREF9.DLL`，关闭全部功能后记录模块基址。
- 记录 D3DREF9 所有已知开关变量的初始值。
- 每次只改变一项功能，开启/关闭之间等待固定时间。

## 阶段 1：D3D9 绘制调用定位

- 在诊断 DLL 中枚举 `IDirect3DDevice9` 虚表。
- 记录 `Present`、`EndScene`、`DrawIndexedPrimitive` 的调用地址、调用频率和调用栈。
- 对 `F7+6` 开启/关闭前后比较绘制状态，定位人物透视绘制分支。

## 阶段 2：实体、骨骼、矩阵数据恢复

需要在实际对局中采样：

- 本地玩家 + 至少一个可见目标。
- 目标静止、移动、死亡各采样一次。
- 记录坐标、阵营、生命值、骨骼候选数组和世界到屏幕矩阵。
- 对候选指针做重复结构聚类和指针链验证。

输出结构：

```json
{
  "entity_list": {"module":"cshell.dll", "rva":"...", "stride":0},
  "bones": {"head":"...", "neck":"...", "chest":"...", "waist":"...", "butt":"..."},
  "view_matrix": {"module":"...", "rva":"..."},
  "health": {"offset":"..."}
}
```

## 阶段 3：功能处理器映射

逐项关联：

```text
D3DREF9 全局开关
→ D3DREF9 读取点
→ 目标对象/绘制/武器调用
→ cshell.dll 数据或函数
```

优先顺序：

1. 人物透视
2. 自动瞄准目标筛选
3. 无后坐力/零秒换弹
4. 子弹穿墙/人物穿墙
5. 不掉血/摔不掉血
6. 移动、第三人称、无限背包

## 阶段 4：接入干净版 DLL

- 为每项功能建立版本签名和 RVA。
- 启动时验证模块版本，不匹配则停用处理器。
- 保留 Home 面板、热键、宏和瞬狙时序。
- 将实际处理器接入 `FeatureManager`，不再使用空状态层。

## 阶段 5：验证

- 每项功能执行：关闭基线→开启→观察→关闭→恢复检查。
- 记录处理器命中、状态变化、绘制/对象结果和恢复结果。
- 生成最终 `feature-adapter-table.json` 和测试报告。

## 当前需要的用户操作

1. 启动固定版本客户端并进入实际对局，而不是只停留在大厅。
2. 保持原 `D3DREF9.DLL` 正常加载。
3. 在场景中准备至少一个可见目标。
4. 告知进入对局后，我再运行实体/矩阵采样器。
