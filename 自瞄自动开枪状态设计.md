# 自瞄 / 自动开枪状态设计

## 总开关

`左Alt+Z` 控制 `AimAutoFire`。关闭时普通模式和瞬狙模式都不得改准心或发送开枪输入。

## 普通模式（`L` 切换）

每帧由实体适配器提供候选对象：

1. 丢弃无效、死亡和本地玩家对象。
2. 阵营模式：只保留 `entity.team != local.team`。
3. 个人模式：按对象逐个筛选，不使用阵营批量过滤。
4. 做障碍判断、屏幕投影和瞄准范围过滤。
5. 按 `P` 选择颈部 / 头部 / 胸部。
6. 目标进入有效范围后，自动瞄准并按开枪速度触发自动开枪。

## 瞬狙模式

瞬狙不是“任意鼠标点击就开枪”。正确触发条件为：

```text
AimAutoFire=ON
瞬狙模式=ON
存在有效敌方目标
鼠标左键或右键按下
当前不在 1600ms 冷却
```

通过后，目标部位从头部、颈部、胸部、中腰、臀部中选择距离准心最近者，执行：

```text
右键↓ → 随机 0..100ms → 左键↓
左键↓ → 随机 0..100ms → 3↓↑
3↓↑ → 35ms → 1↓↑
剩余时间冷却，整轮 1600ms
```

冷却期间只暂停自瞄和自动开枪，不暂停键盘宏。

## 当前版本边界

实体列表、阵营字段、屏幕投影和骨骼读取器尚未接入，因此 `TargetingReady` 默认为 `false`。新构建不会在无目标时发送 `3/1` 序列；接入真实目标适配器后再将其置为 `true`。
