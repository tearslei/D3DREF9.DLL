# D3DREF9 功能与选项目录（截图校正版）

## 自动瞄准

- 热键：`Alt+1`
- 使用类型下拉项：鼠标左键磁性自瞄、鼠标右键磁性自瞄、鼠标左右键磁性自瞄等
- 瞄准部位：头部、颈部、胸部、中腰、臀部
- 瞄准范围：微型（截图当前值）
- 选项：瞄准最近、阵营判断、子弹追踪、障碍判断
- 当前需求：普通模式默认颈部，`P` 循环颈部→头部→胸部；瞬狙模式五部位自动选择距离准心最近者；障碍判断自动开启；保留鼠标左键磁性自瞄。

## 自动开枪

- 开始：`F7+Q`
- 停止：`F7+W`
- 开枪模式：阵营模式、个人模式
- 开枪速度：`0.02` 秒
- 复选项：确保可用、保卫等待

## 功能列表补充

截图确认存在 `F10+8/9/0/D/E/G/H/I/J/K`、`F11+1~7` 等项目。`F9+o` 规范为 `F9+O`，`F10+i` 规范为 `F10+I`，`F9+RF9+T` 拆分为 `F9+R` 与 `F9+T`。

这些 UI 选项不一定对应 D3DREF9 的普通布尔变量，需要单独记录控件消息、配置块和处理线程。
