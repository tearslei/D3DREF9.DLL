# 直接替换版 D3DREF9.DLL：当前还需要的工作

## 已确认

- 客户端目标为 Win32/x86。
- 同版本原始 DLL 备份（`E:\game\已加速- CF2.0搭建（使用2012系统）\客户端\D3DREF9wg.DLL`）只有一个导出：`ordinal 1`，RVA `0x4A7E`。
- 原 ordinal 1 的入口序列为 `push esi; push edi; push ebx; call ...; pop ebx; pop edi; pop esi; ret`，可作为无参数入口的初步 ABI 线索。
- 已生成直接替换产物 `D3DREF9.DLL`：仅导出 `ordinal 1`，Win32/x86；自治 DLL 的命名导出仅保留在 `d3dref9自治.dll` 目标中。
- 当前实际对局曾从 `C:\Windows\System32\D3DREF9.DLL` 加载，说明客户端目录中的同名 DLL 当时缺失或未被搜索到。

## 成品必须完成的 5 个 Gate

### Gate 1：ordinal 1 兼容层（静态已通过，运行时待验证）

在自治 DLL 中加入 `.def` 导出：

```text
ordinal 1 = 无参数初始化入口
```

入口先启动自治模块，再返回原调用方；当前实现为无参数 `__cdecl`、普通 `RET`，返回 `BOOL` 状态。必须在副本客户端中验证进程能继续进入大厅和对局。

### Gate 2：D3D9 初始化链

记录原 DLL ordinal 1 的调用者、返回后模块状态和线程创建情况，确认新入口不会改变栈平衡、寄存器约定或初始化顺序。

### Gate 3：实体/骨骼/矩阵数据源

对当前 `crossfire.exe 1.1.85.7` 获取可重复的：

```text
EntityRoot / Count / Stride
Skeleton pointer / bone indices
View-Projection matrix
```

只接受经过生命周期和屏幕坐标交叉验证的数据，不把扫描到的浮点数直接写入处理器。

### Gate 4：绘制入口

从 `CreateDevice` 返回的真实 `IDirect3DDevice9*` 读取虚表，验证：

```text
Present = vtable[17]
EndScene = vtable[42]
DrawIndexedPrimitive = vtable[82]
```

再把目标模型的 Draw 调用与实体对象关联。

### Gate 5：处理器与回归验证

逐项接入并验证开关关闭后的恢复：

```text
人物透视 → 自瞄筛选 → 无后坐力/换弹 → 穿墙/移动 → 不掉血/摔伤 → 宏/瞬狙
```

每项必须有 before/on/off 三份记录和崩溃回滚点。

## 现在需要用户做的事情

1. 保持当前 CF 对局运行，不要删除或覆盖任何 DLL。
2. 退出对局后保留客户端目录结构；当前目录应确认是否存在：

```powershell
Test-Path 'E:\game\已加速- CF2.0搭建（使用2012系统）\客户端\10.4CrossFire\D3DREF9.DLL'
```

3. 继续使用当前 PID 的只读采样；重启后只需让助手重新获取 PID。
4. 直接替换测试必须使用客户端副本，并保留原始 DLL 备份；不能在原目录上盲替换。

## 交付物顺序

1. `D3DREF9.DLL` ordinal 1 兼容壳（先验证启动）
2. D3D9 入口追踪报告
3. 实体/骨骼/矩阵适配表
4. 处理器适配表和关闭恢复表
5. 最终 x86 `D3DREF9.DLL`、INI、回滚脚本和验证报告

在 Gate 1–4 未通过前，不能把当前 `d3dref9自治.dll` 宣称为完整成品；它目前仍是状态/输入/处理器骨架。
