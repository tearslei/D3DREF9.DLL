# 原 `D3DREF9.DLL` 功能字符串交叉引用（静态证据）

样本：`E:\game\已加速- CF2.0搭建（使用2012系统）\客户端\10.4CrossFire\D3DREF9.DLL`
ImageBase `0x10000000`，x86，SHA-256 `e73fec5c4692d06b136be3d451a88bee05921e0076e260c969f3c97c2a905a5a`。

## 1. 调用链模型

注册器位于 `0x1004D200–0x1004E900`，每项最终调用 `0x1001F30D`。参数中：

- `0x76` = F7 组，`0x78` = F9 组，`0x79` = F10 组；
- 紧邻的立即数是第二键 ASCII；
- 再前面的立即数是 callback；
- callback 多数是 12 字节 wrapper，再跳到实际 handler；
- handler 通过 `and [global],1 / xor [global],1` 或状态机写入开关变量，并调用 UI 更新/初始化/清理函数。

典型链：`F7+6 → 0x10051900 → 0x100517AD → [0x101A8D58]`。

## 2. 用户关心功能的精确映射

|功能文字|热键|注册 callback|实际 handler|主开关/状态变量|结论|
|---|---|---:|---:|---:|---|
|两键优化游戏进程|F7+Y|`0x1004F5B1`|`0x1004EB60`|无单一布尔值（初始化路径）|已注册；不是普通 toggle|
|两键断到专区列表|F7+D|`0x1004F6E4`|`0x1004F5BD`|`0x101A8BE4/8BE8/8BEC`|已注册；状态/流程控制|
|保存房间所有信息|F7+1|`0x1005141C`|`0x1004F6F0`|`0x101A8878`（辅助状态）|已注册|
|开启房间零秒准备|F7+2|`0x10051578`|`0x10051428`|`0x101A8BF4`|已确认 XOR toggle|
|开启房间挂房不掉|F7+3|`0x10051795`|`0x10051584`|`0x101A8D50`|已确认 XOR toggle|
|开启稳定自动准备|F7+4|**未发现注册项**|—|—|字符串存在，但在已扫描注册器中没有 `0x76+0x34`；不能通过该注册路径触发，需动态消息跟踪确认是否存在备用路径|
|开启游戏旧不掉血|F7+5|`0x100517A1`|`0x100124AD`|`0x101A8A88`|已确认 XOR toggle|
|开启游戏人物透视|F7+6|`0x10051900`|`0x100517AD`|`0x101A8D58`|已确认 XOR toggle；绘制/目标处理在后续线程|
|开启游戏幽灵显鬼|F7+7|`0x100519E1`|`0x1005190C`|`0x101A8D5C`|已确认 XOR toggle|
|开启游戏无后坐力|F7+8|`0x10051F25`|`0x100519ED`|`0x101A8D60`|已确认 XOR toggle|
|开启游戏零秒换弹|F7+9|`0x10051F31`|`0x10012661`|`0x101A8A90`|已确认 XOR toggle|
|开启游戏子弹穿墙|F7+0|`0x10051F3D`|`0x10012954`|`0x101A8A98`|已确认 XOR toggle|
|开启游戏刀枪爆头|F9+1|`0x10051F49`|`0x10012DA7`|`0x101A8A9C`|已确认 XOR toggle|
|开启游戏一枪秒杀|F9+2|`0x10051F55`|`0x10013314`|`0x101A8AA0`|已确认 XOR toggle|
|开启游戏子弹加速|F9+3|`0x10051F61`|`0x10014124`|`0x101A8AB0`（1/2/3 状态）|不是简单布尔；状态循环|
|开启游戏名字透视|F9+4|`0x100522CA`|`0x10051F6D`|`0x101A8D28`，并写 `0x101A8D6C`|状态切换+对象/显示初始化|
|开启游戏全屏飞抓|F9+6|`0x100530FF`|`0x100522D6`|`0x101A8D74`|已确认 XOR toggle|
|开启游戏全屏飞踢|F9+7|`0x10053D26`|`0x1005310B`|`0x101A8D88`|已确认 XOR toggle|
|开启游戏全屏飞刀|F9+8|`0x1005451C`|`0x10053D32`|`0x101A8D8C`|已确认 XOR toggle|
|开启游戏全屏枪托|F9+9|`0x10054BF8`|`0x10054528`|`0x101A8D98`|已确认 XOR toggle|
|开启游戏静步加速|F9+A|`0x10054F34`|`0x10054C04`|`0x101A8DA4`|已确认 XOR toggle|
|开启游戏跳跃增强|F9+C|`0x100555AC`|`0x1005527C`|`0x101A8DB4`|已确认 XOR toggle|
|开启游戏空格连跳|F9+D|`0x10055994`|`0x100555B8`|`0x101A8DBC`|已确认 XOR toggle|
|开启游戏人物穿墙|F9+E|`0x10055D7C`|`0x100559A0`|`0x101A8DC4`|已确认 XOR toggle|
|开启游戏人物线条|F9+F|`0x10055E5D`|`0x10055D88`|`0x101A8DCC`|已确认 XOR toggle|
|开启游戏人物变色|F9+G|`0x10055FFE`|`0x10055E69`|`0x101A8DD0`|已确认 XOR toggle|
|开启游戏人物高亮|F9+H|`0x100560DF`|`0x1005600A`|`0x101A8DD4`|已确认 XOR toggle|
|开启游戏无限背包|F9+J|`0x10056491`|`0x10056280`|`0x101A8DE0`|已确认 XOR toggle|
|开启游戏无限喷图|F9+K|`0x10056643`|`0x1005649D`|`0x101A8DEC`|已确认 XOR toggle|
|开启游戏第三人称|F9+L|`0x10056953`|`0x1005664F`|`0x101A8DF4`|已确认；handler 还检查 `0x101A8DFC` 等关联状态|
|开启游戏超级锁枪|F9+N|`0x10056EE2`|`0x10056B2B`|`0x101A8E04`|已确认 XOR toggle|
|开启游戏快速扔雷|F9+P|`0x1005715B`|`0x10056EEE`|`0x101A8E10`|已确认 XOR toggle|
|开启游戏快速切枪|F9+Q|`0x10057413`|`0x10057167`|`0x101A8E18`|已确认 XOR toggle|
|开启游戏早出三秒|F9+R|`0x1005741F`|`0x10014583`|`0x101A8ABC`|已确认 XOR toggle|
|开启游戏高空直走|F9+T|`0x100575E5`|`0x1005742B`|`0x101A8E0C`|已确认 XOR toggle|
|开启游戏摔不掉血|F9+V|`0x1003505D`|`0x100348A7`|`0x101A8C24`|handler 含房间/状态条件|
|开启游戏重刀电锯|F9+X|`0x100577DF`|`0x100575F1`|`0x101A8E24`|已确认|
|开启游戏人物装死|F9+Y|`0x10059993`|`0x10058185`|`0x101A8E44`|已确认|
|开启游戏蹲键遁地（用户称瞬移通地）|F10+W|`0x10062BA9`|`0x1006293C`|`0x101A8F20`|原 DLL 文本是“蹲键遁地”，不是“瞬移通地”|
|开启游戏刷无线电|F10+X|`0x10062DD3`|`0x10062BB5`|`0x101A8F28`|原 DLL 文本对应 F10+X；另有 F10+Y 自动刷无线电项|

## 3. 自动瞄准/自动开枪链

自动开枪的界面记录在 `0x10169776`，字符串表里写的是 `F9+W`；开始/停止记录在 `0x10172BEF/0x10172AF7`，但注册器实际把开始/停止项放在 F7 组，存在文字与注册表不一致：

- `F7+Q → 0x1004EA41`（开始自动开枪路径）
- `F7+W → 0x1004EB3C`（停止/退出路径需结合消息状态确认）

自动瞄准数据表位于 `0x101744D3` 附近，包含瞄准部位、范围和鼠标触发选项。静态文件中没有把这些中文字段直接引用到普通代码，说明由通用 UI 数据表/控件 ID 分发。

## 4. 共同 handler 结构

大部分 handler 都是：

```asm
and dword ptr [global], 1
xor dword ptr [global], 1
cmp dword ptr [global], 0
je  disable_path
; 更新状态文本/控件
; 调用初始化、线程或目标处理器
```

例如 `人物透视`：

```text
0x10051900 → 0x100517AD
0x100517AD: xor [0x101A8D58],1
0x100517AD: ... 调用 0x1000C1BE/0x10001684 维护对象/显示状态
```

`F9+3` 特殊：`0x10014124` 读取/更新 `[0x101A8AB0]` 为 1、2、3，并改变状态颜色/范围，不是布尔开关。

## 5. 目标进程读写与 Hook 证据

已确认导入及调用点：

- `OpenProcess` IAT `0x10114194`，调用 `0x100EEE8B`；
- `VirtualAlloc` IAT `0x101141D8`，调用 `0x100FBE60/0x100FBEEC/0x100FC2BF/0x100FC6C1`；
- `CreateThread` 调用 `0x100BDA33`（入口 `0x100BDA80`）和 `0x100BFF70`（入口 `0x100C0170`）；
- `SetWindowsHookExA` 调用 `0x101059B4`（type 5，callback `0x1010578C`）和 `0x10108856`（type -1，callback `0x10108C18`）；
- `keybd_event` 封装 `0x100EEC50`，`mouse_event` 封装 `0x100EECC0`；
- 动态解析 `GetAsyncKeyState`，解析函数约 `0x1002B1E6`，函数指针保存在 `[0x101A8B90]`。

`cshell.dll` 字符串位于 `0x101181BA`，其代码引用为 `0x10005701`；`CrossFire` 字符串引用 `0x10005B9F`。这些函数负责目标发现/模块定位，但静态样本没有固定写死 `cshell.dll+offset` 的完整补丁表。

## 6. 证据边界

- 上表的 callback、wrapper、handler 和开关变量均来自注册器与代码交叉引用；F7 组及 F10+W/X 有直接文本/顺序对应，F9 后半组部分名称按 UI 记录与注册顺序对齐，置信度为中等。
- “handler 被调用”不等于“目标游戏地址已写入”；真正的实体、骨骼、矩阵、血量、后坐力等地址需要运行时调试/内存快照才能确认。
- 当前客户端未运行，尚未取得 `crossfire.exe/cshell.dll 1.1.85.7` 的实时模块基址、指令字节和写入轨迹。
- `F7+4` 稳定自动准备在注册器缺项，是目前最明确的兼容性问题。

## 7. 下一步可执行采集

1. 在目标私服启动后记录 `crossfire.exe` 和 `cshell.dll` 基址；
2. 对每个 handler 设置断点/硬件写断点，记录 `VirtualProtect/WriteProcessMemory/内联写入` 的目标地址与原/新字节；
3. 按功能逐个切换，导出 `[0x101A8xxx]` 状态变化和目标模块写入日志；
4. 针对 `人物透视/不掉血/穿墙/无后坐力/换弹` 建立版本化 JSON：模块、RVA、原字节、新字节、启用/停用恢复字节；
5. 再以该 JSON 做干净 DLL 的处理器适配。

已生成只读运行时采集器：`tools/runtime_patch_capture.py`。它对 `crossfire.exe/cshell.dll/D3DREF9.DLL` 的可读页建立基线，在单次功能切换后输出模块 RVA、原字节和新字节。PowerShell 的 `$PID` 是只读自动变量，运行时应使用 `--wait` 或自定义变量 `$cfPid`。
