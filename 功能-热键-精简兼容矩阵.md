# 功能、热键与精简候选兼容矩阵（当前实现）

## 当前 DLL（`d3dref9自治.dll` / 直接替换壳）

### 自动启动/隐藏

| 功能 | 热键 | 启动要求 |
|---|---|---|
| 两键优化游戏进程 | 无（面板隐藏） | DLL 加载后约 150 秒自动开启 |
| 房间挂房不卡 | 无（面板隐藏） | Worker 启动后自动开启 |
| 摔不掉血 | 无（面板隐藏） | Worker 启动后自动开启 |

旧不掉血与无限背包已从当前版本删除，不再注册热键、面板项目或自动处理器。

### 面板可切换，默认关闭

| 功能 | 热键 |
|---|---|
| 刷无线电 | F10+X |
| 人物透视 | F7+6 |
| 无后坐力 | F7+8 |
| 零秒换弹 | F7+9 |
| 子弹穿墙 | F7+0 |
| 刀枪爆头 | F9+1 |
| 第三人称 | F9+L |
| 空格连跳 | F9+D |
| 人物穿墙 | F9+E |
| 瞬移通地 | F10+W |

### 自瞄、自动开枪、瞬狙

| 项目 | 设置 |
|---|---|
| 总开关 | 左 Alt+Z；普通模式仅在物理按住鼠标左键时工作 |
| 模式切换 | L，仅切换普通/瞬狙 |
| 普通模式部位 | F10+6 循环：颈部 → 头部 → 胸部 |
| 普通自动开火 | 约每 130 ms 一次 `LEFTDOWN → LEFTUP`，不是长按 |
| 瞬狙触发 | 仅物理鼠标右键；无 F6、无 Alt+1 |
| 瞬狙候选部位 | 头部、颈部、胸部、中腰、臀部，选择准心最近的有效部位 |
| 目标过滤 | 存活、敌我、微型范围、身体骨点相交、障碍射线（fail-closed） |
| 瞬狙冷却 | 冷却期间暂停普通自瞄/自动开枪，结束后仅恢复触发前原本开启状态 |

### 键盘宏和面板

| 项目 | 设置 |
|---|---|
| F9+H | 显示/隐藏左上角 320×420、约 45% 透明面板；Home 已取消；不重置状态 |
| 面板隐藏 | 功能和宏继续运行 |
| 键盘宏暂停 | `[` |
| 宏急停 | `-`，取消当前动作并释放 DLL 自己模拟的按键 |

宏动作沿用 `程序自动化按键.ahk` 的 W+F、W+C、W+左Alt、Ctrl 单次循环、X/V 时序；DLL 不读取或覆盖 AHK 文件。

## `D3DREF9_精简候选.dll`

精简候选只保留原 DLL 的 ordinal 1 入口并阻断已定位调用点。它不包含当前 DLL 的 Home、Alt+Z、L、F10+6、普通 130 ms 点击、瞬狙和宏状态层，不能与当前 DLL 的新热键矩阵混用。
