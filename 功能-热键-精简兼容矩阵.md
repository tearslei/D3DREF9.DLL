# 功能、热键与精简候选兼容矩阵

## 用户最终需求

### 红框：自动启动/隐藏

| 功能 | 热键 | 启动要求 |
|---|---|---|
| 两键优化游戏进程 | F7+Y | 游戏启动约 150 秒后自动启动，不显示在 Home 面板 |
| 房间挂房不卡 | F7+3 | 自动启动，不显示在 Home 面板 |
| 游戏旧不掉血 | F7+5 | 自动启动，不显示在 Home 面板 |
| 游戏摔不掉血 | F9+V | 自动启动 |
| 游戏无限背包 | F9+J | 自动启动 |

### 黄框：面板可切换，默认关闭

| 功能 | 热键 |
|---|---|
| 刷无线电 | F10+X |
| 人物透视 | F7+6 |
| 无后坐力 | F7+8 |
| 零秒换弹 | F7+9 |
| 子弹穿墙 | F7+0 |
| 刀枪爆头 | F9+1 |
| 第三人称 | F9+L |
| 空格连跳 | F9+D |
| 人物穿墙 | F9+E |
| 瞬移通地 | F10+W |

### 自瞄、自动开枪、瞬狙

| 项目 | 设置 |
|---|---|
| 总开关 | 左 Alt+Z，同时控制自动瞄准和自动开枪，默认关闭 |
| 模式切换 | L，仅切换普通模式/瞬狙模式 |
| 普通模式部位 | P 循环：颈部 → 头部 → 胸部 → 颈部 |
| 瞬狙触发 | 鼠标左键或右键，二者触发相同流程 |
| 瞬狙部位 | 头部、颈部、胸部、中腰、臀部，自动选距离准心最近部位 |
| 瞬狙时序 | 右键↓ → 随机 0–100ms → 左键↓ → 随机 0–100ms → 3↓↑ → 35ms → 1↓↑ → 冷却 |
| 瞬狙冷却 | 冷却期间仅暂停自瞄和自动开枪，宏不暂停；总轮次目标 1600ms |

### 键盘宏和面板

| 项目 | 设置 |
|---|---|
| 键盘宏 | 融合进 DLL，O 开关，默认开启 |
| `-` 键 | 删除退出/释放功能，不注册 |
| Home | 左上角约 45% 透明面板，鼠标切换黄框功能 |

## 与 `D3DREF9_精简候选.dll` 的兼容性

| 项目 | 结果 | 说明 |
|---|---|---|
| 原始 ordinal 1/入口 | 兼容性最好 | 直接来自原 DLL，未改入口和导出目录 |
| 原外挂机器码 | 保留 | 精简只改已定位 API 调用点 |
| 指定 IOC 字符串 | 已清除 | 已知域名和外部程序名称不再出现 |
| 网络/注册表/外部命令 | 已阻断部分调用 | 导入表仍保留，间接解析或未定位路径尚未证明不存在 |
| 红框功能 | 可能保留 | 机器码保留，但自动启动和热键需按原 DLL 实机验证 |
| 黄框功能 | 可能保留 | 功能代码保留，但不能保证与你列出的热键一一对应 |
| F7/F9/F10 新映射 | 不保证 | 精简候选使用原 DLL 的输入处理；新映射只写在 `d3dref9自治.dll` |
| Home 45% 面板 | 不兼容/未提供 | 精简候选不是新面板 DLL |
| 左 Alt+Z 总开关 | 不兼容/未提供 | 仅新 DLL 有状态层实现 |
| L/P 模式和部位切换 | 不兼容/未提供 | 原 DLL 的旧热键逻辑未被重映射 |
| O 宏融合 | 不兼容/未提供 | 精简候选保留原代码，不包含新 DLL 的宏实现 |
| 瞬狙 1600ms 新时序 | 不保证 | 需要确认原 DLL 内是否已有相同状态机 |

## 结论

精简候选适合先验证“原外挂机器码还能否运行”；它不能同时保证新的 Home、Alt+Z、L、P、O 和全部新热键。若候选能正常实现游戏功能，下一步应在候选内部重映射输入，而不是同时加载两个 DLL，否则两个全局钩子和宏线程可能冲突。
