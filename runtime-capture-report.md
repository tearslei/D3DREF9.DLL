# CF 运行时功能采集报告

目标进程：`crossfire.exe` PID `12648`  
客户端版本：`crossfire.exe/cshell.dll 1.1.85.7`  
采集范围：`crossfire.exe`、`cshell.dll`、`D3DREF9.DLL` 可读用户态页，以及 D3DREF9 内部开关变量。

## 基线修正

第一轮“只读整页快照”会混入游戏自身的计时器、渲染、网络和对象更新，因此出现大量无关变化（单次可达数百段）。这类变化不能直接当作外挂补丁。

本轮已改为：

1. 先将 CF 窗口置前。
2. 每个热键单独建立基线。
3. 发送一次组合键。
4. 采集 150 ms 短时快照和稳定快照。
5. 直接读取 D3DREF9 已知全局开关 RVA。
6. 对二态功能再次发送一次恢复，并记录 restored 阶段。
7. `F9+3` 按状态序列循环采集。

采集脚本：

`C:\Users\15135\Documents\Codex\2026-08-31\jian\outputs\d3dref9自治\tools\runtime_hotkey_batch.py`

结果目录：

`C:\Users\15135\Documents\Codex\2026-08-31\jian\runtime-diffs\batch\`

## 已确认的开关变化

| 热键 | 变量 RVA | 观察结果 |
|---|---:|---|
| F7+2 | `D3DREF9.DLL+0x1A8BF4` | `00→01`，恢复 `01→00` |
| F7+3 | `+0x1A8D50` | `00→01`，恢复 `01→00` |
| F7+5 | `+0x1A8A88` | `00→01`，恢复 `01→00` |
| F7+6 | `+0x1A8D58` | `00→01`，恢复 `01→00` |
| F7+7 | `+0x1A8D5C` | `00→01`，恢复 `01→00` |
| F7+8 | `+0x1A8D60` | `00→01`，恢复 `01→00` |
| F7+9 | `+0x1A8A90` | `00→01`，恢复 `01→00` |
| F7+0 | `+0x1A8A98` | `00→01`，恢复 `01→00` |
| F9+1 | `+0x1A8A9C` | `00→01`，恢复 `01→00` |
| F9+2 | `+0x1A8AA0` | `00→01`，恢复 `01→00` |
| F9+4 | `+0x1A8D28`、关联 `+0x1A8D6C` | 两个变量同步变化 |
| F9+6/7/8/9 | `+0x1A8D74/88/8C/98` | 各自出现 `00→01` |
| F9+A/C/D/E/F/G/H | 对应 `+0x1A8DA4` 至 `+0x1A8DD4` | 各自出现 `00→01` |
| F9+J/K/L/N/O/P/Q/R/T/Y | 对应已知 RVA | 各自出现 `00→01` |
| F10+W/X | `+0x1A8F20/+0x1A8F28` | 各自出现 `00→01` |

注意：整页差分里的其它 `crossfire.exe/cshell.dll` 变化多数是游戏运行时自然变化，必须结合变量、写入调用栈或硬件写断点进一步归因。

## F9+3 多状态

已连续采样 4 次：

```text
2 → 3 → 0 → 1 → 2
```

因此它不是普通布尔开关，而是状态循环；当前已回到原状态 `2`。

## 自动瞄准与自动开枪

### Alt+1 自动瞄

发送 `Alt+1` 后，D3DREF9 普通功能变量没有发生二态切换，但捕获到 D3DREF9 内部 UI/时序数据写入：

- `D3DREF9.DLL+0x1A89D4: 00→10`
- `D3DREF9.DLL+0x1A8B18: 0000000000→0100000007`
- `D3DREF9.DLL+0x1A8B24: 00→01`
- `D3DREF9.DLL+0x1A8B34: 0000→140E`
- 另有 `+0x1E439C/+0x1E43A4/+0x1E4420` 等界面/时序区域变化

这说明 Alt+1 走的是独立自瞄配置/状态路径，不应使用普通 F7/F9 开关变量判断。

### F7+Q/F7+W 自动开枪

`F7+Q` 采集到：

- `D3DREF9.DLL+0x1A8CDC: 00→01`
- `D3DREF9.DLL+0x1A8CE8` 配置块被初始化
- `+0x1B511C/+0x1B5190` 等时序数据更新

`F7+W` 采集到：

- `+0x1A8CDC: 01→00`
- 配置块停止更新

因此开始/停止链已单独验证，不能按普通 toggle 的“同一功能重复开关”解释。

## 当前状态

已执行恢复动作：

- F7+2、F9+4、F7+6、F7+7、F7+8 均确认当前为 `0`。
- F9+3 已恢复到原始状态 `2`。
- F7+Q 已由 F7+W 停止，`+0x1A8CDC=0`。
- Alt+1 只改变了自瞄独立状态区，需通过 UI 或再次 Alt+1 明确确认其复位语义。

## 结论

这轮可以确认“热键→D3DREF9 内部开关/状态”的闭环；但整页差分不能单独证明 `cshell.dll` 中实体、骨骼、血量、碰撞或武器处理器的最终补丁地址。要还原这些处理器，下一步必须对具体 handler 设置 `WriteProcessMemory/VirtualProtect` 断点或记录写入调用栈。

## 2026-08-31 重启后轮询

新 PID：`2328`。已按“开启→等待 1000 ms→关闭→等待 1500 ms”节奏重新轮询，避免上一轮关闭未完成。已知布尔变量在每个项目关闭后恢复为 `0`。本轮整页/可执行页差分仍出现大量运行时自然变化，因此不能直接把数百段变化视为补丁；需要 API 写入断点或调用栈记录才能筛出真正的 `cshell.dll` 修改。

截图补充确认了自动瞄准和自动开枪的下拉项、复选项及 F11/F10 扩展热键，已记录到 `ui-options-catalog.md`。

## F7+6/F7+8 聚焦复测（PID 2328）

采用 D3DREF9 单模块快照，触发后等待 1200 ms，再关闭并等待 1800 ms：

```text
F7+6: 00→01→00  （+0x1A8D58）
F7+8: 00→01→00  （+0x1A8D60）
```

两次聚焦快照反复出现的其它变化位于 `+0x1B511C/+0x1B5190`，内容每轮都不同，属于运行时计时/配置数据，不是固定补丁字节。聚焦结果文件：`runtime-diffs/focused/F7_6.json`、`runtime-diffs/focused/F7_8.json`。
