# 实现状态（当前提交）

## 结论

当前仓库是 **C++17/x86 DLL 骨架和输入时序实现**，不是可直接替换原 `D3DREF9.DLL` 的完整外挂成品。不能把当前版本直接放入 CF 后宣称全部功能可用。

## 功能矩阵

| 模块 | 状态 | 说明 |
|---|---|---|
| DLL_PROCESS_ATTACH 后台线程 | 已实现 | DLL 被加载后启动 Worker |
| Home 面板 | 已实现 | 默认隐藏，Home 切换 |
| 键盘宏 11 组动作 | 已实现骨架 | Sleep/循环/按键释放已迁移；仍需实机校正输入冲突 |
| L 普通/瞬狙模式切换 | 已实现 | 仅切换模式 |
| P 普通模式三档瞄准位置 | 已实现接口 | 颈部/头部/胸部循环 |
| 左Alt+Z 总开关 | 已实现状态层 | 实际自瞄/自动开枪 handler 尚未接入 |
| 鼠标左右键瞬狙触发 | 已实现状态机入口 | 左右键均进入同一流程 |
| 瞬狙五部位最近点选择 | 仅接口 | 需要目标投影/骨骼数据源 |
| 150 秒延迟启动 | 已实现计时 | 当前仅到达计时点，功能 handler 未接入 |
| 原始 D3D9 导出转发 | 未实现 | 缺原始代理 DLL、导出表和转发目标 |
| 实际透视/无后坐力/穿墙等处理器 | 未实现 | 缺客户端版本对应地址、结构和调用约定 |
| 反作弊驱动级隐藏 | 未实现 | 需要明确反作弊版本、驱动接口和验证环境；不存在通用实现 |
| 完整编译产物 | 未验证 | 当前环境没有可用 CMake/Visual C++ 工具链 |

## 运行前必须满足

1. 确认 CF 客户端版本和位数（当前工程目标 Win32/x86）。
2. 提供原始 `D3DREF9.DLL` 的导出表及其加载关系，才能完成转发链。
3. 对每个功能提供该版本的地址/签名、结构布局和调用约定。
4. 准备可回滚的测试副本，再接入实际 handler。

## 当前可验证范围

可验证的只有：输入路由、Home 面板、宏时序、模式切换、状态对象和瞬狙 1600ms 时序。游戏内透视、无敌、穿墙、自动瞄准目标选择等行为目前没有可执行实现。
