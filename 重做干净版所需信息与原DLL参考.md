# 重做干净版 DLL：可行性、参考证据与所需信息

## 结论

**现实可行，但不是从字符串直接“复刻全部功能”。** 原 `D3DREF9.DLL` 是 x86 原生 PE、无源码、只有 ordinal 1 导出；功能处理器、初始化、UI 和热键都编译在同一个二进制中。可以通过静态逆向和运行时观测逐项恢复，但需要先恢复调用链和客户端数据结构。

## 原 DLL 可以直接参考的内容

目标文件：

```text
E:\game\已加速- CF2.0搭建（使用2012系统）\客户端\10.4CrossFire\D3DREF9.DLL
```

已确认：

| 项目 | 证据 |
|---|---|
| 架构 | x86 PE，ImageBase `0x10000000` |
| 入口 | RVA `0xF23C6` |
| 导出 | 仅 ordinal 1，RVA `0x4A7E` |
| 客户端版本 | `crossfire.exe` FileVersion `1.1.85.7` |
| 功能字符串 | 位于 `.rdata/.data`，可作为 UI/处理器定位锚点 |
| 外联/外部命令 | `WS2_32`、`WININET`、注册表、`ShellExecuteA`、`WinExec` 均有导入 |

已定位的功能字符串（文件偏移）：

```text
自动瞄准       0x1744D3
自动开枪       0x169776
无线电         0x16F355
瞬移           0x16ACCD
第三人称       0x1797B7
人物穿墙       0x179D93
空格连跳       0x179EBF
刀枪爆头       0x17B286
子弹穿墙       0x17B3B6
无后坐力       0x17B616
人物透视       0x17B876
不掉血         0x173381
无限背包       0x17AD7C
挂房           0x17C0D7
两键优化       0x17C08E
摔不掉血       0x178F83
零秒换弹       0x17B4E6
```

这些位置能用于查找引用、定位配置对话框和功能分支；它们**不是游戏数据地址，也不是处理器入口地址**。

## 用户要求的最终接口

### 自动启动项

150 秒后启动：两键优化游戏进程。立即/自动启动：房间挂房不卡、旧不掉血、摔不掉血、无限背包。以上不显示在 Home 面板。

### 面板开关项（默认关闭）

刷无线电、人物透视、无后坐力、零秒换弹、子弹穿墙、刀枪爆头、第三人称、空格连跳、人物穿墙、瞬移通地。

### 自瞄/开枪/瞬狙

- 左 Alt+Z：自动瞄准 + 自动开枪总开关。
- L：普通模式/瞬狙模式切换。
- P：普通模式部位循环：颈部 → 头部 → 胸部。
- 瞬狙：左右鼠标键均触发；部位集合为头部、颈部、胸部、中腰、臀部，选离准心最近者。
- 时序：右键↓ → 随机 0–100ms → 左键↓ → 随机 0–100ms → `3`↓↑ → 35ms → `1`↓↑ → 冷却；总周期 1600ms。
- 瞬狙冷却只暂停自瞄和自动开枪。

### 宏/Home

- Home：左上角 45% 透明面板。
- O：键盘宏开关，默认开。
- `-`：删除退出及释放功能。
- AHK 需要改成 DLL 内部动作表和计时器，不再依赖外部 `.ahk`。

## 为什么不能只靠精简原 DLL 完成新接口

精简原 DLL 可以保留原机器码，但新接口仍有四个硬边界：

1. 原 DLL 只有一个未命名 ordinal 导出，不能直接调用内部功能处理器。
2. 原热键、窗口、配置、网络初始化和功能代码互相交织，不能从导入表安全推导功能边界。
3. 原功能可能直接使用 `crossfire.exe/cshell.dll` 的内部地址；新客户端或加载时机变化会失效。
4. 新 Home、Alt+Z、L、P、O、瞬狙时序需要新的状态机和输入层，原 DLL 中没有这些“新协议”的源码接口。

因此有两条路线：

### 路线 A：原 DLL 内部补丁（最快）

1. 在副本上定位原热键分发函数和每个功能的开关变量。
2. 把热键常量改成用户最终映射。
3. 把外联、注册表和外部命令路径改为本地失败/跳过。
4. 在原 UI 消息循环中增加 Home 面板和开关映射。
5. 逐项在 CF 中验证。

优点：原功能地址和调用约定仍在。缺点：混淆/无符号、补丁脆弱，任何错误都可能使原 DLL 崩溃。

### 路线 B：干净 DLL + 移植处理器（长期）

1. 保留原 DLL 作为参考样本，不加载两个 DLL。
2. 从原 DLL 反汇编恢复每个处理器的读写对象和调用约定。
3. 在 `crossfire.exe`/`cshell.dll` 中定位对应版本结构、渲染矩阵、实体列表和武器状态。
4. 用签名扫描替代硬编码地址。
5. 将处理器接入现有 `FeatureManager`、`InputRouter`、`HomePanel`、`InstantSniper`。

优点：结构干净、热键可控、可移植。缺点：每个功能都需要真实地址/结构验证，不能只靠原 DLL 字符串完成。

## 现在需要收集的最小信息

### 必须由分析获得

- 原 DLL 每个功能字符串的交叉引用函数。
- 功能开关变量的地址、类型和写入位置。
- 原热键分发函数及按键码表。
- `crossfire.exe`/`cshell.dll` 的实体、骨骼、矩阵、生命、武器、碰撞结构。
- 原 DLL ordinal 1 的参数和调用时机。

### 需要用户提供/配合的运行时证据

- 启动 CF 后保持在大厅/房间/对局三个阶段。
- 逐个按原 DLL 的功能键，并记录画面变化。
- 对每个功能提供“开/关”前后现象；不需要提供账号或外网信息。
- 如果某功能只在特定模式生效，记录模式、地图和武器。

## 验收标准

不能以“DLL 被加载”作为完成标准。每个功能必须有：

1. 热键或面板操作记录；
2. 游戏内开/关对照现象；
3. 进程模块和线程稳定性；
4. 关闭功能后的恢复结果；
5. 无外联、无外部命令、无非必要注册表写入的行为验证。

## 最快下一步

先在**原 DLL 副本**上做静态交叉引用和热键分发定位，再决定是路线 A 还是路线 B。当前已经生成的 `D3DREF9_精简候选.dll` 只能验证“原机器码在去掉外联后是否还能启动”，不能自动增加 Home/Alt+Z/L/P/O，也不能证明全部功能已正常。
