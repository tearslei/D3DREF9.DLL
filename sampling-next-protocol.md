# 下一轮采样协议

上一轮 CSV 的采样时序有效，但字段集合不足以确认实体/骨骼/矩阵。下一轮只需要记录动作时间，不需要改 DLL 或发送按键。

## 启动

```powershell
cd 'C:\Users\15135\Documents\Codex\2026-08-31\jian'
python .\outputs\d3dref9自治\tools\auto_entity_timeline.py
```

## 5 分钟窗口

不需要按预设顺序，也不需要让目标执行固定动作。保持游戏正常运行即可；如果自然发生移动、跳跃、换武器或回合变化，记录大概时间即可。

```powershell
python .\outputs\d3dref9自治\tools\auto_entity_timeline.py --duration 300
```

## 可选动作记录

| 相对时间 | 操作 |
|---:|---|
| 0–20s | 本地玩家不动，保持一个可见目标在屏幕内 |
| 20–50s | 只移动本地玩家，目标保持可见 |
| 50–80s | 只让目标移动 |
| 80–110s | 只让目标跳跃/落地 |
| 110–140s | 让目标离开视野，再回到视野 |
| 140–170s | 目标死亡或回合结束（若可行） |
| 170–180s | 停止操作，保持画面 |

## 记录要求

只需要把实际发生的时间点记下来，例如：`目标开始移动 52s`、`目标跳跃 83s`、`目标死亡 146s`。采样结束后回复这些时间点。

## 当前轮次判定标准

- 时长 ≥ 170s、采样间隔中位数 ≤ 0.15s：时序合格。
- 目标动作前后出现持续、可重复、成组变化：进入候选实体布局分析。
- 只有固定 RVA 变化或 `-100000` 哨兵值：仍不能接入实体处理器，需要下一轮扩展内存候选扫描。
