# TCII 2.0 源码与基址工具

## 源码位置

`E:\game\辅助\2.0 TCII全套开源` 内的 `.e/.ec` 为易语言工程，工程口令为 `7`。当前项目已将主工程导出到 `source-dumps/`，并生成：

- `source-feature-index.md/json`：功能与源码行索引
- `source-hotkey-table.md/json`：热键映射
- `source-patch-table.json`：写入点和处理器线索
- `config/client_1.1.85.7.json`：1.1.85.7 版本适配常量
- `config/source-base-catalog.json`：从源码声明提取的 94 个基址/偏移（含文件与行号）

### 生成/更新基址目录

源码是易语言容器，不能直接作为 PDB 使用。先从已导出的文本生成可审计目录：

```powershell
python .\tools\generate_base_catalog.py
```

该命令只读扫描 `source-dumps/*.txt`，输出每个 `cshell.dll + RVA` 或命名常量的
来源行号；不会写入游戏进程。运行时适配仍必须经过版本签名和模块基址校验。

## 基址/版本检查

在 CF 退出时执行：

```powershell
cd 'C:\Users\15135\Documents\Codex\2026-08-31\jian\outputs\d3dref9自治'
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\verify_client_signature.ps1
```

CF 运行时再次执行同一命令，会输出 PID、`crossfire.exe/cshell.dll/D3DREF9.DLL` 模块基址和大小。配置中的 RVA 通过 `模块基址 + RVA` 换算，禁止把旧版本绝对地址直接套用到新版本。

源码中没有单独发布“基址工具”或教程工程；可用的基址资料就是易语言主工程中的初始化表和注释。当前适配器按主工程 `TCII变态版本(加追敌和跳舞).e` 使用以下链：

```text
cshell + 0x166AD00 -> 玩家数组指针
玩家数组 + 0x210  -> OBJECT/人物表
OBJECT + 0x14 + (slot-1)*0xD80 -> 实体指针
玩家数组 + 0x30 -> 本地人物一级（+0x44/+0x48 角度，+0x6C/+0x70/+0x74 坐标）
玩家数组 + 0x208 -> 本地槽位索引（结果 +1）
```

坐标表由 `crossfire+0x23567F` 的签名保护 relay 捕获，捕获后按
`table[(slot-1)] -> +0x0C + part*0x40` 读取五部位（头/颈/胸/腰/臀 = 5/6/4/3/2）。

## 构建和部署

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\部署直接替换壳到客户端.ps1 -Apply
```

脚本会先把客户端旧 DLL 保存为 `D3DREF9.before-ordinal1.bak`。回滚：

```powershell
Copy-Item 'E:\game\已加速- CF2.0搭建（使用2012系统）\客户端\10.4CrossFire\D3DREF9.before-ordinal1.bak' `
  'E:\game\已加速- CF2.0搭建（使用2012系统）\客户端\10.4CrossFire\D3DREF9.DLL' -Force
```

## 运行诊断

进入实际对局 10 秒以上后退出 CF，查看：

```powershell
$t=[IO.Path]::GetTempPath()
Get-Content (Join-Path $t 'd3dref9_entity_hook.log') -Tail 40
Get-Content (Join-Path $t 'd3dref9_render.log') -Tail 40
Import-Csv (Join-Path $t 'd3dref9_entities.csv') |
  Select-Object -Last 30 tick,coord_table,entity_slot,entity_addr,alive,enemy,pos_valid,bone_mask,screen_valid,screen_x,screen_y
```

`event=installed/capture/device_captured/draw_indexed_primitive` 和非零 `pos_valid/bone_mask/screen_valid` 表明实体、骨骼、矩阵及屏幕投影链已经在该次运行建立；若缺少某事件，依据对应日志定位签名或初始化时序，而不是继续盲扫地址。

## 成品构建与回滚

`build.ps1` 会同时生成两份 Win32 产物：

| 文件 | 用途 |
|---|---|
| `d3dref9自治.dll` | 独立加载/调试，保留完整导出接口 |
| `D3DREF9.DLL` | 客户端直接替换壳，仅导出 ordinal 1 |

直接替换前必须确认客户端已退出。部署脚本自动创建
`D3DREF9.before-ordinal1.bak`；出现启动问题时执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\restore_original_d3dref9.ps1
```
