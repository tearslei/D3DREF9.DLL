# D3DREF9 热键验证清单（1.1.85.7）

## 结论

不需要每个热键都机械地执行“开启→关闭”。按处理器行为分类：

- **布尔开关**：开启一次、关闭一次（2 次采样）。
- **一次性动作/流程**：只触发一次（1 次采样），观察调用、线程、写入和 UI 反馈。
- **多状态功能**：连续触发并记录状态序列，直到回到初态。
- **开始/停止成对功能**：分别触发开始和停止，不把它们当同一个 toggle。
- **未知或未注册项**：先做一次触发确认是否进入处理器，不盲目重复。

`F7+6`（人物透视）已经完成开启/关闭闭环：`00→01→00`。

## 必须开启/关闭各一次

### F7 组

`F7+2`、`F7+3`、`F7+5`、`F7+6`、`F7+7`、`F7+8`、`F7+9`、`F7+0`

### F9 组（已在注册器中确认有处理器的项目）

`F9+1`、`F9+2`、`F9+4`、`F9+6`、`F9+7`、`F9+8`、`F9+9`、`F9+A`、`F9+C`、`F9+D`、`F9+E`、`F9+F`、`F9+G`、`F9+H`、`F9+J`、`F9+K`、`F9+L`、`F9+N`、`F9+O`、`F9+P`、`F9+Q`、`F9+R`、`F9+T`、`F9+U`、`F9+V`、`F9+W`、`F9+X`、`F9+Y`、`F9+Z`

> 这些键是否是严格二态，仍以采样到的写入值为准；若第二次触发不是恢复原值，则改按“多状态/动作”记录。

### F10 组

`F10+W`、`F10+X` 已有主开关变量；其余 `F10+1/2/3/4/5/6/7/8/9/0/D/E/G/H/I/J/K/M/N/Q/R/Z` 先按未知处理器各触发一次。

## 只触发一次的动作/流程

- `F7+Y`：两键优化游戏进程
- `F7+D`：断到专区列表
- `F7+1`：保存房间所有信息
- `F11+1`～`F11+7`：先确认是否存在备用注册/消息路径，再各触发一次

## 多状态/成对功能

- `F9+3`：记录完整序列（通常为 `1→2→3→1`），不要只做“开/关”。
- `F7+Q`：自动开枪开始；`F7+W`：自动开枪停止。分别测试一次。
- 自动瞄准：面板选项与鼠标/F 键触发单独记录，不纳入 F7/F9/F10 二态表。

## 单次采样步骤

1. 让目标进程稳定 1–2 秒并建立基线。
2. 只触发一个热键一次。
3. 立即记录：处理器命中、状态变量、目标模块写入、线程/Hook 变化、UI 文本。
4. 对二态项再触发一次，确认是否恢复；对动作项不重复触发。
5. 每个功能单独保存 JSON，避免多个功能同时改变导致差分无法归因。

## 规范化输入

- `F9+o` → `F9+O`
- `F10+i` → `F10+I`
- `F9+RF9+T` → `F9+R`、`F9+T`
- 用户列表中的 `F11+4`～`F11+7` 当前静态注册器未发现，需走备用消息路径确认。
