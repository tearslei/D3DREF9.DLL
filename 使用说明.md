# d3dref9自治.dll 使用说明

## 1. 启动和部署

目标客户端是 Win32/x86。退出 `crossfire.exe` 后，在工程目录执行：

```powershell
cd 'C:\Users\15135\Documents\Codex\2026-08-31\jian\outputs\d3dref9自治'
.\\部署直接替换壳到客户端.ps1       # 只预览
.\\部署直接替换壳到客户端.ps1 -Apply  # 复制并保留回滚备份
```

备份文件为客户端目录中的 `D3DREF9.before-ordinal1.bak`。启动客户端后可验证实际加载路径：

```powershell
.\\tools\\verify_client_d3dref9_load.ps1
```

如果使用独立加载方式，不要重复加载第二份 DLL；只加载工程生成的 `d3dref9自治.dll`。

## 2. 按键说明

| 按键 | 功能 |
|---|---|
| F9+H | 显示/隐藏左上角 320×420、约 45% 透明面板；原 Home 键已取消；不重置状态 |
| F7+5 | 旧版不掉血开关；启动时默认开启，不显示在面板 |
| F9+H 面板状态行 | 显示当前开枪模式，以及 Alt+Z 自瞄/自动开火状态（[开]/[关]）；状态仅由 Alt+Z 切换 |
| 左Alt+Z | 自瞄/自动开火总开关；第一次开启，第二次关闭 |
| L | 普通模式/瞬狙模式切换 |
| F10+6 | 普通瞄准位置循环：颈部、头部、胸部 |
| F9+V | 摔不掉血开关；启动时默认开启，按一次切换 |
| 普通模式物理左键 | 按住才运行自瞄和自动开火；松开立即停止 |
| 瞬狙模式物理右键 | 按住持续触发瞬狙；松开立即停止 |
| [ | 暂停/恢复键盘宏 |
| - | 取消宏并释放 DLL 自己模拟的按键 |

`Alt+1`、`F6`、`P` 均已删除，不再注册。

## 3. 普通自瞄和开火

1. 按 `左Alt+Z` 打开总开关。
2. 保持物理鼠标左键按下。
3. DLL 只选择存活敌人，并执行敌我判断、准心附近筛选、微型范围、五骨点身体相交和障碍射线。
4. 目标有效后写入瞄准角度。
5. 自动开火间隔约 130 ms，每次发送一次 `LEFTDOWN → LEFTUP`，不是持续按住。
6. 角度写入后默认等待 35 ms 再点击，防止同一帧尚未完成瞄准就开火。

## 4. 瞬狙

1. 按 `L` 切到瞬狙模式。
2. 物理按下鼠标右键触发；无有效目标时不会发送鼠标序列。
3. 使用中型准心范围（`instant_range_divisor=8`），从头部、颈部、胸部、中腰、臀部中选择离准心最近的有效点。
4. 冷却期间普通自瞄/自动开火暂停；冷却结束只恢复触发前原本开启的状态。
5. 开火后自动执行 `3`（约 45 ms）→ 等待约 100 ms → `1`（约 25 ms）；该链完成后再等待 300 ms，才恢复普通自瞄/自动开火。
6. 时序参数见 `config\\d3dref9自治.ini` 的 `[instant_sniper]`。
7. 默认启用开火前急停：暂时释放物理 WASD 25 ms，重新取目标并写入角度，完成射击链后 35 ms 恢复移动；可在配置中关闭或调节。

## 5. 键盘宏

宏已按原 AHK 时序迁移到 DLL，包含 W+F、W+C、W+左Alt 及 X/V 转向。`W+Ctrl` 组合已删除，不再触发 Ctrl 单次循环。新宏会取消旧宏，退出时只释放 DLL 自己按下的键。DLL 不读取、不覆盖 `程序自动化按键.ahk`。

## 6. 自动启动功能

- 房间挂房不卡、旧版不掉血、摔不掉血：Worker 启动后自动开启且不显示在面板；旧版不掉血用 F7+5 切换，摔不掉血用 F9+V 切换。
- 两键优化游戏进程：DLL 加载约 150 秒后自动开启且不显示在面板。
- 无限背包：当前版本已删除。

## 7. 诊断日志

```powershell
Get-Content "$env:TEMP\\d3dref9_input.log" -Tail 80
Get-Content "$env:TEMP\\d3dref9_handlers.log" -Tail 80
Get-Content "$env:TEMP\\d3dref9_render.log" -Tail 80
```

实体采样写入 `%TEMP%\\d3dref9_entities.csv`。若某项运行时地址或签名不匹配，处理器保持关闭并记录日志，不写入未知地址。


