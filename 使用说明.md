# d3dref9自治.dll 使用说明

## 1. 当前启动链

`d3dref9自治.dll` 的自动启动分为两层：

1. **加载层**：由宿主/注入器把 DLL 加载进 `crossfire.exe`；DLL 本身不会主动寻找并注入游戏。
2. **功能层**：DLL 加载后创建后台线程、键盘/鼠标输入路由和隐藏 Home 面板；检测到工作线程启动后计时 150 秒，再执行配置中的启动阶段。

因此，想让 CF 启动时自动启动外挂，必须先让 DLL 在 CF 进程中自动加载。

## 2. 方案 A：接入现有 D3D9 代理链

适用于你已有 `D3DREF9.DLL` 代理加载方式的客户端：

1. 先编译本工程，得到 `d3dref9自治.dll`。
2. 在代理 DLL 的转发链中加载 `d3dref9自治.dll`。
3. 保留原始 `D3DREF9.DLL` 的全部导出函数转发。
4. 将代理 DLL 放到 CF 客户端实际搜索目录。
5. 启动 CF，确认 DLL 被加载后，等待 150 秒。
6. 游戏内按 `Home`，左上角才显示面板。

当前工程本身**尚未实现原 D3D9 导出转发**，不能直接把它重命名覆盖原 `D3DREF9.DLL`；需要先接入你现有的代理壳或补齐导出转发。

## 3. 方案 B：使用已有 DLL 注入器

适用于你已经在使用的本地注入器：

1. 启动 CF 客户端。
2. 注入器目标选择 `crossfire.exe`。
3. DLL 选择：

```text
C:\Users\15135\Documents\Codex\2026-08-31\jian\outputs\d3dref9自治\build\Release\d3dref9自治.dll
```

4. 注入完成后不需要再启动 `.ahk` 文件。
5. 等待 150 秒，自动启动项进入工作状态。
6. 按 `Home` 打开面板。

## 4. 默认按键

| 按键 | 功能 |
|---|---|
| `Home` | 显示/隐藏面板，默认隐藏 |
| `左Alt+Z` | 自动瞄准 + 自动开枪总开关，默认关闭 |
| `L` | 普通模式/瞬狙模式切换 |
| `P` | 普通模式瞄准位置：颈部 → 头部 → 胸部 |
| `O` | 键盘宏总开关，默认开启 |
| `鼠标左键 / 鼠标右键` | 瞬狙模式触发磁性自瞄与瞄准射击 |
| `-` | 未注册，不退出、不释放按键 |

## 5. 面板行为

- Home 面板固定左上角，背景 alpha 约 55%（视觉透明度约 45%）。
- 面板默认隐藏，不影响功能状态。
- 鼠标点击面板行与热键使用同一状态对象。
- `左Alt+Z` 对应行默认显示“关”。
- 瞬狙模式显示“自动选择准心最近部位”。
- 瞬狙冷却只暂停自动瞄准和自动开枪，键盘宏继续运行。

## 6. 正常运行时的可见行为

当前版本已处理：

- 不创建控制台窗口。
- Home 面板默认隐藏。
- 不读取或启动 `程序自动化按键.ahk`。
- 不访问此前删除的外联 IOC。
- 不写注册表、不创建开机启动项。
- 删除调试输出，150 秒计时在后台执行。
- DLL 卸载时释放自身创建的键盘/鼠标钩子和注入按键。

当前版本尚未实现：

- D3D9 原始导出函数转发。
- 针对反作弊驱动的隐藏、Hook 伪装或 `SendInput` 痕迹处理。
- 游戏特征地址对应的透视、无后坐力等实际 handler。

所以目前的“看不出”范围是：无窗口、无控制台、无外部 AHK、无网络 IOC；不能把当前骨架称为完全不可检测版本。

## 7. 编译

```powershell
cd 'C:\Users\15135\Documents\Codex\2026-08-31\jian\outputs\d3dref9自治'
.\build.ps1
```

生成后检查：

```powershell
Get-Item '.\build\Release\d3dref9自治.dll'
```

## 8. 配置

配置文件：

```text
config\d3dref9自治.ini
```

其中：

```ini
[startup]
delay_ms=150000

[instant_sniper]
trigger_mouse=left_or_right
```

瞬狙触发固定为鼠标左键或鼠标右键，两者触发相同的瞬狙流程。

