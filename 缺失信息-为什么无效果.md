# 为什么加载后没有游戏效果

## 直接原因

你加载的是工程新写的 `d3dref9自治.dll`。它目前只包含输入路由、Home 面板、宏和瞬狙状态机；它没有向 CF 的实体、武器、生命或渲染对象写入任何地址，所以不会产生透视、无后坐力、不掉血、穿墙等游戏效果。

这不是加载失败，而是**功能处理器尚未接入**。

## 最快可行路径

原始 `D3DREF9.DLL` 本身已经包含原外挂的编译代码。相比重写全部功能，最快路线是在它的副本上做精简：

```text
D3DREF9_精简候选.dll
```

该候选保留原 PE 入口、ordinal 1、资源和原功能代码，只对已定位的网络、注册表、`ShellExecuteA`、`WinExec` 调用做失败桩处理，并清除已知 IOC。它才是当前用于验证“原外挂功能是否仍在”的文件，不是 `d3dref9自治.dll`。

## 仍缺少的不是“一个地址”

要把功能迁移到干净 DLL，需要从当前客户端（`crossfire.exe` 1.1.85.7 / `cshell.dll`）确认：

1. 目标实体列表、阵营、坐标和骨骼/碰撞结构。
2. 世界到屏幕矩阵、D3D9 设备或渲染回调，用于人物透视和方框。
3. 武器对象、后坐力参数、换弹/射击状态和输入调用约定。
4. 生命值、受伤处理、碰撞/穿墙相关函数及其调用约定。
5. 房间对象和网络消息结构（房间不卡、零秒准备等）。
6. 当前版本实际加载顺序，以及原 DLL ordinal 1 的调用参数。

缺少这些数据时，只能做“界面和热键状态”，不能可靠实现游戏内修改。

## 当前应加载哪个文件

- 测试原外挂功能：使用 `D3DREF9_精简候选.dll`，通过部署脚本放到客户端目录。
- 测试新界面/宏：使用 `d3dref9自治.dll`，通过 `启动-自治DLL.ps1` 独立加载。

两者不是同一个实现，不能期待新界面 DLL 自动拥有原外挂功能。
