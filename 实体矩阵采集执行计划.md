# 实体、骨骼、矩阵与 D3D9 入口采集执行计划

## 当前已执行
- 对运行时模块进行只读采样：`crossfire.exe`、`cshell.dll`、`d3dref9.dll`。
- 原 DLL 功能状态引用静态扫描完成；每个功能的状态变量消费点已导出。
- D3D9 COM 设备候选扫描完成一次：当前非对局状态下候选数为 0。
- 新一轮 600 秒实体/矩阵时间线采集已启动，PID 10236。

## 进入对局后的证据要求
1. 至少一个可见目标保持在视野内 120 秒以上。
2. 目标执行静止、移动、跳跃/下蹲中的任意两种动作。
3. 采样期间不重启 CF，不切换 D3DREF9 文件。
4. 记录进入对局和退出对局时间点，避免把大厅/退场数据当成实体数据。

## 实体候选判定
- 候选必须在连续时间窗内变化，并且在相邻地址处出现重复记录步长。
- 候选需在场景切换后创建/销毁，与目标数量变化一致。
- 单个浮点变化、相机角度变化或退场时归零，不计为实体。

## 骨骼与矩阵判定
- 矩阵需满足连续 16-float 结构、齐次项约束和稳定更新。
- 骨骼需与同一实体坐标同步变化，并能在多个骨骼槽位形成固定步长。
- 必须通过至少两次独立对局窗口复现，才写入版本适配表。

## D3D9 绘制入口判定
- 设备指针必须在运行中稳定存在。
- 记录 `Present(17)`、`EndScene(42)`、`DrawIndexedPrimitive(82)` 的地址与调用频率。
- 将绘制调用与 `F7+6` 状态切换时间线做相关性分析；仅有静态状态引用不算绘制入口。

## 接入门槛
只有同时满足“实体生命周期 + 骨骼步长 + 世界矩阵 + 绘制入口”四项证据，才允许生成处理器适配表；此前结果只保留为候选，不写入 DLL。
